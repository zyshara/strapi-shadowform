'use strict';

/**
 * wedding-invite-token service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::wedding-invite-token.wedding-invite-token');
