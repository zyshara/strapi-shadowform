'use strict';

/**
 * wedding-invite-token router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::wedding-invite-token.wedding-invite-token');
