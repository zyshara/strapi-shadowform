'use strict';

/**
 * wedding-invite-token controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::wedding-invite-token.wedding-invite-token');
