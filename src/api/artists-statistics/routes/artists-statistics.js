'use strict';

/**
 * artists-statistics router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::artists-statistics.artists-statistics');
